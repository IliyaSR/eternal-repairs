import javax.swing.*;

public class UpdateCarWindow extends JFrame {
    UpdateCarWindow(){
        setTitle("Update car window");
        setSize(700, 950);
        setDefaultCloseOperation(EXIT_ON_CLOSE);
        setLocationRelativeTo(null);

        setVisible(true);
    }

}
